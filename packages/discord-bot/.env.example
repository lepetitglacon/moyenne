# =============================================================================
# Discord Bot Configuration - Dokploy Deployment
# =============================================================================

# =============================================================================
# REQUIRED - Bot will crash if missing
# =============================================================================

# PostgreSQL Database URL (Dokploy PostgreSQL instance - SAME as server)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://moyenne:YOUR_PASSWORD@dokploy-postgres-host:5432/moyenne

# Discord bot token (from Discord Developer Portal)
DISCORD_TOKEN=your_discord_bot_token

# Discord application client ID (from Discord Developer Portal)
DISCORD_CLIENT_ID=your_discord_client_id

# API key to authenticate with the server (must match BOT_API_KEY on server)
API_KEY=your_api_key_change_in_production

# =============================================================================
# Optional
# =============================================================================

# Server API base URL (point to your Dokploy server service)
# For Dokploy internal network: http://server-service-name:3000/api
# For external: https://your-domain.com/api
API_BASE_URL=http://localhost:3000/api

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO
