# =============================================================================
# Discord Bot Configuration
# =============================================================================

# =============================================================================
# REQUIRED - Bot will crash if missing
# =============================================================================

# Discord bot token (from Discord Developer Portal)
DISCORD_TOKEN=your_discord_bot_token

# Discord application client ID (from Discord Developer Portal)
DISCORD_CLIENT_ID=your_discord_client_id

# API key to authenticate with the server (must match BOT_API_KEY on server)
API_KEY=your_api_key

# =============================================================================
# Optional
# =============================================================================

# Server API base URL (default: http://localhost:3000/api)
API_BASE_URL=http://localhost:3000/api

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO

# Database path - for Docker volume persistence (default: ./bot.sqlite)
# DB_PATH=/app/data/bot.sqlite
