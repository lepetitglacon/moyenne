# =============================================================================
# Monorepo Moyenne - Configuration
# =============================================================================
#
# ⚠️ DOKPLOY DEPLOYMENT : Services run via Dockerfiles directly
#
# Configure .env files in each package directory:
#
#   packages/server/.env      → Backend Express
#   packages/discord-bot/.env → Bot Discord
#   packages/frontend/.env    → Frontend Vue/Vite
#
# =============================================================================

# =============================================================================
# PostgreSQL Configuration (Dokploy Managed Database)
# =============================================================================
# ⚠️ Set this to your Dokploy PostgreSQL connection string
DATABASE_URL=postgresql://moyenne:YOUR_PASSWORD@dokploy-postgres-host:5432/moyenne

# =============================================================================
# Server Configuration
# =============================================================================
SERVER_PORT=3000
# ⚠️ CHANGE IN PRODUCTION!
JWT_SECRET=your_jwt_secret_here_change_in_production
# ⚠️ CHANGE IN PRODUCTION! (must match bot's API_KEY)
DISCORD_BOT_API_KEY=your_bot_api_key_here_change_in_production

# =============================================================================
# Discord Bot Configuration
# =============================================================================
DISCORD_TOKEN=your_discord_bot_token
DISCORD_CLIENT_ID=your_discord_client_id
# Point to your server service (internal Dokploy network or public URL)
API_BASE_URL=http://server-service:3000
